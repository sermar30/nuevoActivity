import React from 'react';
import ReactDOM from 'react-dom';
import { AppAC } from './components/AppAC';

ReactDOM.render(
  <React.StrictMode>
    <AppAC />
  </React.StrictMode>,
  document.getElementById('root')
);
